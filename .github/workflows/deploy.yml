name: Deploy SAP Job Alert Bot

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/sap-job-alert:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Optional: SSH deploy to a VPS
      # - name: Deploy to VPS
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.VPS_HOST }}
      #     username: ${{ secrets.VPS_USER }}
      #     key: ${{ secrets.VPS_SSH_KEY }}
      #     script: |
      #       docker pull ${{ secrets.DOCKERHUB_USERNAME }}/sap-job-alert:latest
      #       docker stop sap_job_alert || true
      #       docker rm sap_job_alert || true
      #       docker run -d --name sap_job_alert \
      #         --restart unless-stopped \
      #         -v job_data:/data \
      #         --env-file /opt/sap-alert/.env \
      #         ${{ secrets.DOCKERHUB_USERNAME }}/sap-job-alert:latest
